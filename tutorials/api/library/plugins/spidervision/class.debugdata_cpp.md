# Unigine::Plugins::SpiderVision::DebugData Class (CPP)

**Header:** #include <plugins/Unigine/SpiderVision/UnigineSpiderVision.h>


Этот объект хранит отладочные данные, такие как состояние [warp grid](../../../../principles/render/output/multi_monitor/spidervision_plugin/projection_setup.md#warp), точки настройки [blend zone](../../../../principles/render/output/multi_monitor/spidervision_plugin/projection_setup.md#blend), заливку [debug color](../../../../principles/render/output/multi_monitor/spidervision_plugin/projection_setup.md#render_options) viewport и т.д.


> **Notice:** Отладочные данные **не** хранятся в файле конфигурации.


Этот объект доступен через соответствующий метод класса [ViewportData](../../../../api/library/plugins/spidervision/class.viewportdata_cpp.md#getDebug_DebugData).


## DebugData Class

### Методы класса

## void setShowBlendLines ( bool lines )

Задаёт новое значение, указывающее, включена ли визуализация контрольных линий зоны смешивания и выбранных точек на экране.
### Аргументы

- *bool* **lines** - Установите **true**, чтобы включить визуализацию контрольных линий и точек зоны смешивания; **false** - чтобы отключить её.

## bool isShowBlendLines () const

Возвращает текущее значение, указывающее, включена ли визуализация контрольных линий зоны смешивания и выбранных точек на экране.
### Возвращаемое значение

**true**, если визуализация контрольных линий и точек зоны смешивания включена; иначе **false**.
## void setBlendPointPositions ( const Vector < Math:: vec2 >& positions )

Задаёт новые позиции точек зоны смешивания, отображаемых в viewport.
### Аргументы

- *const [Vector](../../../../api/library/containers/vector/class.vector_cpp.md)< Math::[vec2](../../../../api/library/math/class.vec2_cpp.md)>&* **positions** - Вектор, содержащий позиции точек зоны смешивания, отображаемых в viewport.

## Vector < Math:: vec2 > getBlendPointPositions () const

Возвращает текущие позиции точек зоны смешивания, отображаемых в viewport.
### Возвращаемое значение

Текущий вектор, содержащий позиции точек зоны смешивания, отображаемых в viewport.
## void setShowGrid ( bool grid )

Задаёт новое значение, указывающее, включена ли визуализация сетки искажения на экране.
### Аргументы

- *bool* **grid** - Установите **true**, чтобы включить визуализацию сетки искажения; **false** - чтобы отключить её.

## bool isShowGrid () const

Возвращает текущее значение, указывающее, включена ли визуализация сетки искажения на экране.
### Возвращаемое значение

**true**, если визуализация сетки искажения включена; иначе **false**.
## void setWarpPointsEnabled ( bool enabled )

Задаёт новое значение, указывающее, отображаются ли контрольные точки искажения в viewport.
### Аргументы

- *bool* **enabled** - Установите **true**, чтобы включить отображение контрольных точек искажения; **false** - чтобы отключить его.

## bool isWarpPointsEnabled () const

Возвращает текущее значение, указывающее, отображаются ли контрольные точки искажения в viewport.
### Возвращаемое значение

**true**, если отображение контрольных точек искажения включено; иначе **false**.
## void setWarpPointPositions ( const Vector < Math:: vec2 >& positions )

Задаёт новые позиции контрольных точек сетки искажения, отображаемых в viewport.
### Аргументы

- *const [Vector](../../../../api/library/containers/vector/class.vector_cpp.md)< Math::[vec2](../../../../api/library/math/class.vec2_cpp.md)>&* **positions** - Вектор, содержащий позиции точек сетки искажения, отображаемых в viewport.

## Vector < Math:: vec2 > getWarpPointPositions () const

Возвращает текущие позиции контрольных точек сетки искажения, отображаемых в viewport.
### Возвращаемое значение

Текущий вектор, содержащий позиции точек сетки искажения, отображаемых в viewport.
## void setDebugColorIndex ( int index )

Задаёт новый индексированный цвет, используемый в режиме отладки.
> **Notice:** Задание индивидуальных цветов для разных viewport позволяет визуализировать перекрывающиеся области разных viewport и упрощает процесс настройки.


### Аргументы

- *int* **index** - Цвет, используемый в режиме отладки, одно из следующих значений:

  - **0** - без цвета
  - **1** - красный
  - **2** - зелёный
  - **3** - синий
  - **4** - голубой
  - **5** - пурпурный
  - **6** - жёлтый
  - **7** - белый
  - **8** - чёрный

## int getDebugColorIndex () const

Возвращает текущий индексированный цвет, используемый в режиме отладки.
> **Notice:** Задание индивидуальных цветов для разных viewport позволяет визуализировать перекрывающиеся области разных viewport и упрощает процесс настройки.


### Возвращаемое значение

Текущий цвет, используемый в режиме отладки, одно из следующих значений:
- **0** - без цвета
- **1** - красный
- **2** - зелёный
- **3** - синий
- **4** - голубой
- **5** - пурпурный
- **6** - жёлтый
- **7** - белый
- **8** - чёрный


## Math:: vec4 getDebugColor () const

Возвращает текущий цвет, используемый в режиме отладки.
> **Notice:** Задание индивидуальных цветов для разных viewport позволяет визуализировать перекрывающиеся области разных viewport и упрощает процесс настройки.


### Возвращаемое значение

Текущий цвет, используемый в режиме отладки.
## static Event<> getEventChanged () const

событие, вызываемое при изменении отладочных данных. Вы можете подписаться на события через *connect()* и отписаться через *disconnect()*. Для удобства также можно использовать классы *[EventConnection](../../../../api/library/common/events/class.eventconnection_cpp.md)* и *[EventConnections](../../../../api/library/common/events/class.eventconnections_cpp.md)* (см. примеры ниже).

> **Notice:** Подробнее см. статью [Event Handling](../../../../code/fundamentals/events/index_cpp.md).

 Сигнатура обработчика события выглядит следующим образом: *myhandler()*
<details>
<summary>Показать пример | Скрыть</summary>

**Пример использования**

```cpp
// реализация обработчика события Changed
void changed_event_handler()
{
	Log::message("\Обработка события Changed\n");
}

//////////////////////////////////////////////////////////////////////////////
//  1. Несколько подписок можно связать с экземпляром класса EventConnections,
//  который затем можно использовать для отмены всех этих подписок сразу
//////////////////////////////////////////////////////////////////////////////

// создаём экземпляр класса EventConnections
EventConnections changed_event_connections;

// связываем с этим экземпляром при подписке на событие (можно связывать подписки на разные события)
DebugData::getEventChanged().connect(changed_event_connections, changed_event_handler);

// другие подписки также связаны с этим экземпляром EventConnections
// (например, можно подписываться с помощью лямбда-функций)
DebugData::getEventChanged().connect(changed_event_connections, []() {
		Log::message("\Обработка события Changed (лямбда).\n");
	}
);

// ...

// позже все эти связанные подписки можно удалить одной строкой
changed_event_connections.disconnectAll();

//////////////////////////////////////////////////////////////////////////////
//  2. Можно подписываться и отписываться через экземпляр класса EventConnection.
//  А также включать и отключать это конкретное соединение при необходимости.
//////////////////////////////////////////////////////////////////////////////

// создаём экземпляр класса EventConnection
EventConnection changed_event_connection;

// подписываемся на событие Changed с функцией-обработчиком, сохраняя соединение
DebugData::getEventChanged().connect(changed_event_connection, changed_event_handler);

// ...

// можно временно отключить конкретное соединение события для выполнения определённых действий
changed_event_connection.setEnabled(false);

// ... выполняемые действия

// и снова включить его при необходимости
changed_event_connection.setEnabled(true);

// ...

// удаляем подписку на событие Changed через соединение
changed_event_connection.disconnect();

//////////////////////////////////////////////////////////////////////////////
//  3. Можно добавить экземпляр EventConnection/EventConnections как член класса,
//  обрабатывающего событие. В этом случае все связанные подписки будут
//  автоматически удалены при вызове деструктора класса
//////////////////////////////////////////////////////////////////////////////

// Класс, обрабатывающий событие
class SomeClass
{
public:
	// экземпляр класса EventConnections как член класса
	EventConnections e_connections;

	// Обработчик события Changed, реализованный как член класса
	void event_handler()
	{
		Log::message("\Обработка события Changed\n");
		// ...
	}
};

SomeClass *sc = new SomeClass();

// ...

// указываем экземпляр класса, если метод-обработчик принадлежит какому-либо классу
DebugData::getEventChanged().connect(sc->e_connections, sc, &SomeClass::event_handler);

// ...

// экземпляр класса-обработчика удаляется, а все его подписки автоматически отменяются
delete sc;

//////////////////////////////////////////////////////////////////////////////
//   4. Подписка на событие с сохранением идентификатора соединения
//   и последующей отпиской по этому идентификатору
//////////////////////////////////////////////////////////////////////////////
// экземпляр класса EventConnections для управления соединениями событий
EventConnections e_connections;

// определяем идентификатор соединения, который будет использован для отписки позже
EventConnectionId changed_handler_id;

// подписываемся на событие Changed с лямбда-обработчиком, сохраняя идентификатор соединения
changed_handler_id = DebugData::getEventChanged().connect(e_connections, []() {
		Log::message("\Обработка события Changed (лямбда).\n");
	}
);

// удаляем подписку позже, используя идентификатор
DebugData::getEventChanged().disconnect(changed_handler_id);

//////////////////////////////////////////////////////////////////////////////
//   5. Игнорирование всех событий Changed при необходимости
//////////////////////////////////////////////////////////////////////////////

// можно временно отключить событие для выполнения определённых действий без его срабатывания
DebugData::getEventChanged().setEnabled(false);

// ... выполняемые действия

// и снова включить его при необходимости
DebugData::getEventChanged().setEnabled(true);

```

</details>

### Возвращаемое значение

Ссылка на событие.
---

## String getDebugColorName ( int index ) const

Возвращает имя отладочного цвета по его индексу.
### Аргументы

- *int* **index** - Индекс цвета, используемого в режиме отладки.

### Возвращаемое значение

Имя индексированного цвета.
## int getNumDebugColors ( ) const

Возвращает общее количество индексированных отладочных цветов.
### Возвращаемое значение

Общее количество индексированных отладочных цветов.
## void save ( const Ptr < Stream > & stream )

Сохраняет отладочные данные в указанный поток.
### Аргументы

- *const [Ptr](../../../../api/library/common/class.ptr_cpp.md)<[Stream](../../../../api/library/common/class.stream_cpp.md)> &* **stream** - Поток, в который записываются данные.

## void restore ( const Ptr < Stream > & stream )

Загружает отладочные данные из указанного потока.
### Аргументы

- *const [Ptr](../../../../api/library/common/class.ptr_cpp.md)<[Stream](../../../../api/library/common/class.stream_cpp.md)> &* **stream** - Поток, из которого загружаются данные.
