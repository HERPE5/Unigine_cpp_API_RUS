# Unigine::Plugins::SpiderVision::CalibrationGridData Class (CPP)

**Header:** #include <plugins/Unigine/SpiderVision/UnigineSpiderVision.h>


[Calibration Grid](../../../../principles/render/output/multi_monitor/spidervision_plugin/calibration.md) хранит данные о настройках калибровочной сетки текущей конфигурации.


Калибровочная сетка представляет собой сетку определённой формы (сфера или параллелепипед) с заданным расстоянием между линиями этой сетки. Сетка помогает выравнивать изображения, отрисовываемые проекторами, для получения бесшовного изображения.


> **Notice:** Данные калибровочной сетки **не** хранятся в файле конфигурации.


Этот объект доступен через соответствующий метод класса [DisplaysConfig](../../../../api/library/plugins/spidervision/class.displaysconfig_cpp.md#getCalibrationGrid_CalibrationGridData).


## CalibrationGridData Class

### Перечисления

## CALIBRATION_GRID_TYPE

| Name | Description |
|---|---|
| **CALIBRATION_GRID_TYPE_SPHERE** = 0 | Сферическая калибровочная сетка. |
| **CALIBRATION_GRID_TYPE_BOX** = 1 | Калибровочная сетка в форме параллелепипеда. |
| **CALIBRATION_GRID_TYPE_COLOR** = 2 | Калибровочная сетка, залитая цветом. |

### Методы класса

## void setVisible ( bool visible )

Задаёт новое значение, указывающее, отрисовывается ли калибровочная сетка.
### Аргументы

- *bool* **visible** - Установите **true**, чтобы включить отрисовку калибровочной сетки; **false** - чтобы отключить её.

## bool isVisible () const

Возвращает текущее значение, указывающее, отрисовывается ли калибровочная сетка.
### Возвращаемое значение

**true**, если отрисовка калибровочной сетки включена; иначе **false**.
## void setDistance ( float distance )

Задаёт новое расстояние от точки обзора до сетки. Разница заметна при использовании калибровочного шаблона в форме параллелепипеда.
### Аргументы

- *float* **distance** - Расстояние от точки обзора до сетки.

## float getDistance () const

Возвращает текущее расстояние от точки обзора до сетки. Разница заметна при использовании калибровочного шаблона в форме параллелепипеда.
### Возвращаемое значение

Текущее расстояние от точки обзора до сетки.
## void setType ( CalibrationGridData::CALIBRATION_GRID_TYPE type )

Задаёт новый тип калибровочного шаблона.
### Аргументы

- *[CalibrationGridData::CALIBRATION_GRID_TYPE](../../../../api/library/plugins/spidervision/class.calibrationgriddata_cpp.md#CALIBRATION_GRID_TYPE)* **type** - Тип калибровочного шаблона.

## CalibrationGridData::CALIBRATION_GRID_TYPE getType () const

Возвращает текущий тип калибровочного шаблона.
### Возвращаемое значение

Текущий тип калибровочного шаблона.
## void setSphereCut ( bool cut )

Задаёт новое значение, указывающее, включена ли опция Sphere Cut для сферической калибровочной сетки. Эта опция позволяет скрыть верхний и нижний полюса.
### Аргументы

- *bool* **cut** - Установите **true**, чтобы включить опцию Sphere Cut; **false** - чтобы отключить её.

## bool isSphereCut () const

Возвращает текущее значение, указывающее, включена ли опция Sphere Cut для сферической калибровочной сетки. Эта опция позволяет скрыть верхний и нижний полюса.
### Возвращаемое значение

**true**, если опция Sphere Cut включена; иначе **false**.
## void setLinesVerticalStep ( float step )

Задаёт новое расстояние между основными вертикальными линиями.
### Аргументы

- *float* **step** - Расстояние между основными вертикальными линиями.

## float getLinesVerticalStep () const

Возвращает текущее расстояние между основными вертикальными линиями.
### Возвращаемое значение

Текущее расстояние между основными вертикальными линиями.
## void setLinesHorizontalStep ( float step )

Задаёт новое расстояние между основными горизонтальными линиями.
### Аргументы

- *float* **step** - Расстояние между основными горизонтальными линиями.

## float getLinesHorizontalStep () const

Возвращает текущее расстояние между основными горизонтальными линиями.
### Возвращаемое значение

Текущее расстояние между основными горизонтальными линиями.
## void setLinesVerticalMinorCount ( int count )

Задаёт новое количество вспомогательных линий между двумя соседними основными вертикальными линиями.
### Аргументы

- *int* **count** - Количество вспомогательных линий между двумя соседними основными вертикальными линиями.

## int getLinesVerticalMinorCount () const

Возвращает текущее количество вспомогательных линий между двумя соседними основными вертикальными линиями.
### Возвращаемое значение

Текущее количество вспомогательных линий между двумя соседними основными вертикальными линиями.
## void setLinesHorizontalMinorCount ( int count )

Задаёт новое количество вспомогательных линий между двумя соседними основными горизонтальными линиями.
### Аргументы

- *int* **count** - Количество вспомогательных линий между двумя соседними основными горизонтальными линиями.

## int getLinesHorizontalMinorCount () const

Возвращает текущее количество вспомогательных линий между двумя соседними основными горизонтальными линиями.
### Возвращаемое значение

Текущее количество вспомогательных линий между двумя соседними основными горизонтальными линиями.
## void setTextEnabled ( bool enabled )

Задаёт новое значение, указывающее, отрисовывается ли текст на калибровочной сетке.
### Аргументы

- *bool* **enabled** - Установите **true**, чтобы включить отрисовку текста на калибровочной сетке; **false** - чтобы отключить её.

## bool isTextEnabled () const

Возвращает текущее значение, указывающее, отрисовывается ли текст на калибровочной сетке.
### Возвращаемое значение

**true**, если отрисовка текста на калибровочной сетке включена; иначе **false**.
## void setTextFontSize ( int size )

Задаёт новый размер текста, отображаемого на калибровочной сетке.
### Аргументы

- *int* **size** - Размер текста, отображаемого на калибровочной сетке.

## int getTextFontSize () const

Возвращает текущий размер текста, отображаемого на калибровочной сетке.
### Возвращаемое значение

Текущий размер текста, отображаемого на калибровочной сетке.
## void setLineHighlightEnabled ( bool enabled )

Задаёт новое выделение одной горизонтальной и одной вертикальной линии.
### Аргументы

- *bool* **enabled** - Установите **true**, чтобы включить выделение одной горизонтальной и одной вертикальной линии; **false** - чтобы отключить его.

## bool isLineHighlightEnabled () const

Возвращает текущее выделение одной горизонтальной и одной вертикальной линии.
### Возвращаемое значение

**true**, если выделение одной горизонтальной и одной вертикальной линии включено; иначе **false**.
## void setLineHighlightHorizontalIndex ( int index )

Задаёт новый индекс горизонтального выделения. Может быть выделена только одна линия. Линия невидима, если её индекс равен нулю.
### Аргументы

- *int* **index** - Индекс горизонтального выделения.

## int getLineHighlightHorizontalIndex () const

Возвращает текущий индекс горизонтального выделения. Может быть выделена только одна линия. Линия невидима, если её индекс равен нулю.
### Возвращаемое значение

Текущий индекс горизонтального выделения.
## void setLineHighlightVerticalIndex ( int index )

Задаёт новый индекс вертикального выделения. Может быть выделена только одна линия. Линия невидима, если её индекс равен нулю.
### Аргументы

- *int* **index** - Индекс вертикального выделения.

## int getLineHighlightVerticalIndex () const

Возвращает текущий индекс вертикального выделения. Может быть выделена только одна линия. Линия невидима, если её индекс равен нулю.
### Возвращаемое значение

Текущий индекс вертикального выделения.
## void setTransformYaw ( float yaw )

Задаёт новую трансформацию сетки вдоль вертикальной оси. Трансформируется только сетка, проецируемое изображение не трансформируется.
### Аргументы

- *float* **yaw** - Угол yaw, в градусах.

## float getTransformYaw () const

Возвращает текущую трансформацию сетки вдоль вертикальной оси. Трансформируется только сетка, проецируемое изображение не трансформируется.
### Возвращаемое значение

Текущий угол yaw, в градусах.
## void setTransformPitch ( float pitch )

Задаёт новую трансформацию сетки вдоль поперечной оси. Трансформируется только сетка, проецируемое изображение не трансформируется.
### Аргументы

- *float* **pitch** - Угол pitch, в градусах.

## float getTransformPitch () const

Возвращает текущую трансформацию сетки вдоль поперечной оси. Трансформируется только сетка, проецируемое изображение не трансформируется.
### Возвращаемое значение

Текущий угол pitch, в градусах.
## void setTransformRoll ( float roll )

Задаёт новую трансформацию сетки вдоль продольной оси. Трансформируется только сетка, проецируемое изображение не трансформируется.
### Аргументы

- *float* **roll** - Угол roll, в градусах.

## float getTransformRoll () const

Возвращает текущую трансформацию сетки вдоль продольной оси. Трансформируется только сетка, проецируемое изображение не трансформируется.
### Возвращаемое значение

Текущий угол roll, в градусах.
## static Event<> getEventChanged () const

событие, вызываемое при изменении данных калибровочной сетки. Вы можете подписаться на события через *connect()* и отписаться через *disconnect()*. Для удобства также можно использовать классы *[EventConnection](../../../../api/library/common/events/class.eventconnection_cpp.md)* и *[EventConnections](../../../../api/library/common/events/class.eventconnections_cpp.md)* (см. примеры ниже).

> **Notice:** Подробнее см. статью [Event Handling](../../../../code/fundamentals/events/index_cpp.md).

 Сигнатура обработчика события выглядит следующим образом: *myhandler()*
<details>
<summary>Показать пример | Скрыть</summary>

**Пример использования**

```cpp
// реализация обработчика события Changed
void changed_event_handler()
{
	Log::message("\Обработка события Changed\n");
}

//////////////////////////////////////////////////////////////////////////////
//  1. Несколько подписок можно связать с экземпляром класса EventConnections,
//  который затем можно использовать для отмены всех этих подписок сразу
//////////////////////////////////////////////////////////////////////////////

// создаём экземпляр класса EventConnections
EventConnections changed_event_connections;

// связываем с этим экземпляром при подписке на событие (можно связывать подписки на разные события)
CalibrationGridData::getEventChanged().connect(changed_event_connections, changed_event_handler);

// другие подписки также связаны с этим экземпляром EventConnections
// (например, можно подписываться с помощью лямбда-функций)
CalibrationGridData::getEventChanged().connect(changed_event_connections, []() {
		Log::message("\Обработка события Changed (лямбда).\n");
	}
);

// ...

// позже все эти связанные подписки можно удалить одной строкой
changed_event_connections.disconnectAll();

//////////////////////////////////////////////////////////////////////////////
//  2. Можно подписываться и отписываться через экземпляр класса EventConnection.
//  А также включать и отключать это конкретное соединение при необходимости.
//////////////////////////////////////////////////////////////////////////////

// создаём экземпляр класса EventConnection
EventConnection changed_event_connection;

// подписываемся на событие Changed с функцией-обработчиком, сохраняя соединение
CalibrationGridData::getEventChanged().connect(changed_event_connection, changed_event_handler);

// ...

// можно временно отключить конкретное соединение события для выполнения определённых действий
changed_event_connection.setEnabled(false);

// ... выполняемые действия

// и снова включить его при необходимости
changed_event_connection.setEnabled(true);

// ...

// удаляем подписку на событие Changed через соединение
changed_event_connection.disconnect();

//////////////////////////////////////////////////////////////////////////////
//  3. Можно добавить экземпляр EventConnection/EventConnections как член класса,
//  обрабатывающего событие. В этом случае все связанные подписки будут
//  автоматически удалены при вызове деструктора класса
//////////////////////////////////////////////////////////////////////////////

// Класс, обрабатывающий событие
class SomeClass
{
public:
	// экземпляр класса EventConnections как член класса
	EventConnections e_connections;

	// Обработчик события Changed, реализованный как член класса
	void event_handler()
	{
		Log::message("\Обработка события Changed\n");
		// ...
	}
};

SomeClass *sc = new SomeClass();

// ...

// указываем экземпляр класса, если метод-обработчик принадлежит какому-либо классу
CalibrationGridData::getEventChanged().connect(sc->e_connections, sc, &SomeClass::event_handler);

// ...

// экземпляр класса-обработчика удаляется, а все его подписки автоматически отменяются
delete sc;

//////////////////////////////////////////////////////////////////////////////
//   4. Подписка на событие с сохранением идентификатора соединения
//   и последующей отпиской по этому идентификатору
//////////////////////////////////////////////////////////////////////////////
// экземпляр класса EventConnections для управления соединениями событий
EventConnections e_connections;

// определяем идентификатор соединения, который будет использован для отписки позже
EventConnectionId changed_handler_id;

// подписываемся на событие Changed с лямбда-обработчиком, сохраняя идентификатор соединения
changed_handler_id = CalibrationGridData::getEventChanged().connect(e_connections, []() {
		Log::message("\Обработка события Changed (лямбда).\n");
	}
);

// удаляем подписку позже, используя идентификатор
CalibrationGridData::getEventChanged().disconnect(changed_handler_id);

//////////////////////////////////////////////////////////////////////////////
//   5. Игнорирование всех событий Changed при необходимости
//////////////////////////////////////////////////////////////////////////////

// можно временно отключить событие для выполнения определённых действий без его срабатывания
CalibrationGridData::getEventChanged().setEnabled(false);

// ... выполняемые действия

// и снова включить его при необходимости
CalibrationGridData::getEventChanged().setEnabled(true);

```

</details>

### Возвращаемое значение

Ссылка на событие.
## void setBackgroundColor ( const Math:: vec4 & color )

Задаёт новый цвет фона калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет фона калибровочной сетки.

## Math:: vec4 getBackgroundColor () const

Возвращает текущий цвет фона калибровочной сетки.
### Возвращаемое значение

Текущий цвет фона калибровочной сетки.
## void setHighlightedColor ( const Math:: vec4 & color )

Задаёт новый цвет выделенной линии.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет выделенной линии.

## Math:: vec4 getHighlightedColor () const

Возвращает текущий цвет выделенной линии.
### Возвращаемое значение

Текущий цвет выделенной линии.
## void setHorizontalMinorLinesColor ( const Math:: vec4 & color )

Задаёт новый цвет вспомогательных горизонтальных линий калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет вспомогательных горизонтальных линий калибровочной сетки.

## Math:: vec4 getHorizontalMinorLinesColor () const

Возвращает текущий цвет вспомогательных горизонтальных линий калибровочной сетки.
### Возвращаемое значение

Текущий цвет вспомогательных горизонтальных линий калибровочной сетки.
## void setHorizontalMajorLinesColor ( const Math:: vec4 & color )

Задаёт новый цвет основных горизонтальных линий калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет основных горизонтальных линий калибровочной сетки.

## Math:: vec4 getHorizontalMajorLinesColor () const

Возвращает текущий цвет основных горизонтальных линий калибровочной сетки.
### Возвращаемое значение

Текущий цвет основных горизонтальных линий калибровочной сетки.
## void setVerticalMinorLinesColor ( const Math:: vec4 & color )

Задаёт новый цвет вспомогательных вертикальных линий калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет вспомогательных вертикальных линий калибровочной сетки.

## Math:: vec4 getVerticalMinorLinesColor () const

Возвращает текущий цвет вспомогательных вертикальных линий калибровочной сетки.
### Возвращаемое значение

Текущий цвет вспомогательных вертикальных линий калибровочной сетки.
## void setVerticalMajorLinesColor ( const Math:: vec4 & color )

Задаёт новый цвет основных вертикальных линий калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет основных вертикальных линий калибровочной сетки.

## Math:: vec4 getVerticalMajorLinesColor () const

Возвращает текущий цвет основных вертикальных линий калибровочной сетки.
### Возвращаемое значение

Текущий цвет основных вертикальных линий калибровочной сетки.
## void setAxesColor ( const Math:: vec4 & color )

Задаёт новый цвет центральных осей калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет центральных осей калибровочной сетки.

## Math:: vec4 getAxesColor () const

Возвращает текущий цвет центральных осей калибровочной сетки.
### Возвращаемое значение

Текущий цвет центральных осей калибровочной сетки.
## void setTextColor ( const Math:: vec4 & color )

Задаёт новый цвет текста калибровочной сетки.
### Аргументы

- *const  Math::[vec4](../../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет текста калибровочной сетки.

## Math:: vec4 getTextColor () const

Возвращает текущий цвет текста калибровочной сетки.
### Возвращаемое значение

Текущий цвет текста калибровочной сетки.
---

## void save ( const Ptr < Stream > & stream )

Сохраняет данные калибровочной сетки в указанный поток.
### Аргументы

- *const [Ptr](../../../../api/library/common/class.ptr_cpp.md)<[Stream](../../../../api/library/common/class.stream_cpp.md)> &* **stream** - Поток, в который записываются данные.

## void restore ( const Ptr < Stream > & stream )

Загружает данные калибровочной сетки из указанного потока.
### Аргументы

- *const [Ptr](../../../../api/library/common/class.ptr_cpp.md)<[Stream](../../../../api/library/common/class.stream_cpp.md)> &* **stream** - Поток, из которого загружаются данные.
