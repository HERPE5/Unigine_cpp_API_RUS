# Unigine::Plugins::IG::CollisionSegment Class (CPP)

**Header:** #include <plugins/Unigine/IG/UnigineIG.h>


Этот класс используется для управления сегментами столкновений, назначаемыми [entities](../../../../../api/library/plugins/ig/api/class.entity_cpp.md) и используемыми для обнаружения столкновений.

> **Notice:** Плагин IG должен быть загружен.


Сегмент обнаружения столкновений — это отрезок, вдоль которого IG выполняет проверку столкновений. Когда сегмент обнаружения столкновений пересекает полигон, IG регистрирует столкновение, отправляя соответствующее уведомление.

> **Notice:** Проверка обнаружения столкновений выполняется IG каждый кадр.


Сегмент задаётся указанием положений его конечных точек относительно системы координат тела связанной сущности. На рисунке ниже показаны пять сегментов, заданных для самолёта.


![](collision_segments.jpg)


Объёмы обнаружения столкновений проверяются по схеме сегмент-полигон. Сущность не выполняет проверку сегментов обнаружения столкновений относительно собственной геометрии.


Если сущность уничтожена, все сегменты обнаружения столкновений, заданные для этой сущности, также будут уничтожены.


## CollisionSegment Class

### Методы класса

---

## int getID ( ) const

Возвращает ID сегмента столкновения.
### Возвращаемое значение

ID сегмента столкновения.
## Entity * getEntity ( ) const

Возвращает [entity](../../../../../api/library/plugins/ig/api/class.entity_cpp.md), которой назначен сегмент столкновения.
### Возвращаемое значение

Сущность, которой назначен сегмент столкновения.
## bool isEnabled ( ) const

Возвращает значение, указывающее, включён ли сегмент столкновения.
### Возвращаемое значение

true, если сегмент столкновения включён; иначе false
## void setEnabled ( bool value )

Включает или отключает сегмент столкновения.
### Аргументы

- *bool* **value** - true, чтобы включить сегмент столкновения; false - чтобы отключить его.

## void setStartPoint ( const Math::vec3& point )

Задаёт координаты начальной точки сегмента столкновения.
### Аргументы

- *const  Math::vec3&* **point** - Координаты начальной точки сегмента столкновения, в системе координат [entity](#getEntity_Entity).

## const Math::vec3& getStartPoint ( ) const

Возвращает текущие координаты начальной точки сегмента столкновения.
### Возвращаемое значение

Координаты начальной точки сегмента столкновения, в системе координат [entity](#getEntity_Entity).
## void setEndPoint ( Math:: vec3 point )

Задаёт координаты конечной точки сегмента столкновения.
### Аргументы

- *Math::[vec3](../../../../../api/library/math/class.vec3_cpp.md)* **point** - Координаты конечной точки сегмента столкновения, в системе координат [entity](#getEntity_Entity).

## Math:: vec3 getEndPoint ( ) const

Возвращает текущие координаты конечной точки сегмента столкновения.
### Возвращаемое значение

Координаты конечной точки сегмента столкновения, в системе координат [entity](#getEntity_Entity).
## void setMaterialMask ( int mask )

Задаёт маску материалов для сегмента столкновения. Этот параметр определяет природные и антропогенные объекты, учитываемые или исключаемые при проверке столкновений.
### Аргументы

- *int* **mask** - Целое число, представляющее битовую маску, каждый бит которой соответствует диапазону значений кода материала. Установка этого бита в 1 приведёт к тому, что IG будет регистрировать попадания в материалы в соответствующем диапазоне.

## int getMaterialMask ( ) const

Возвращает маску материалов для сегмента столкновения. Этот параметр определяет природные и антропогенные объекты, учитываемые или исключаемые при проверке столкновений.
### Возвращаемое значение

Целое число, представляющее битовую маску, каждый бит которой соответствует диапазону значений кода материала. Установка этого бита в 1 приведёт к тому, что IG будет регистрировать попадания в материалы в соответствующем диапазоне.
## void * addOnCollisionDetectedCallback ( Unigine:: CallbackBase3 < CollisionSegment *, Ptr < Object >, Ptr < WorldIntersection >> * func )

Добавляет функцию колбэка, вызываемую при обнаружении столкновения с сегментом столкновения. Эта функция может использоваться для определения конкретных действий, выполняемых при обнаружении столкновения с сегментом. Сигнатура функции колбэка должна быть следующей:
```cpp
void(CollisionSegment *segment , ObjectPtr object, WorldIntersectionPtr wi);
```


Функцию колбэка можно задать следующим образом:


```cpp
addOnCollisionDetectedCallback(MakeCallback(collision_callback_function_name));
```


**Пример:** Задание колбэка обнаружения столкновения для сегмента столкновения сущности


```cpp
/// функция колбэка, вызываемая при обнаружении столкновения с сегментом столкновения
void my_callback(CollisionSegment *segment , ObjectPtr object, WorldIntersectionPtr wi)
{
	// вставьте здесь код обработки события столкновения
}

// ...
// где-то в коде
void SomeClass::init()
{
	// добавление "my_callback" для вызова при обнаружении столкновения с сегментом
	ig_manager->getEntity(entity_id)->getCollisionSegment(segment_id)->addOnCollisionDetectedCallback(Unigine::MakeCallback( my_callback ));
}

```


### Аргументы

- *Unigine::[CallbackBase3](../../../../../api/library/common/callbacks/class.callbackbase3_cpp.md)< [CollisionSegment](../../../../../api/library/plugins/ig/api/class.collisionsegment_cpp.md) *, [Ptr](../../../../../api/library/common/class.ptr_cpp.md)<[Object](../../../../../api/library/objects/class.object_cpp.md)>, [Ptr](../../../../../api/library/common/class.ptr_cpp.md)<[WorldIntersection](../../../../../api/library/worlds/class.worldintersection_cpp.md)>> ** **func** - Функция колбэка.

### Возвращаемое значение

ID последнего добавленного колбэка обнаружения столкновения, если колбэк был успешно добавлен; иначе **nullptr**. Этот ID можно использовать для [remove](#removeOnCollisionDetectedCallback_void_ptr_bool) этого колбэка при необходимости.
## bool removeOnCollisionDetectedCallback ( void * id )

Удаляет указанный колбэк из списка колбэков обнаружения столкновения.
### Аргументы

- *void ** **id** - ID колбэка обнаружения столкновения, полученный при его [adding](#addOnCollisionDetectedCallback_CallbackBase_ptr_void).

### Возвращаемое значение

True, если колбэк обнаружения столкновения с указанным ID был успешно удалён; иначе false.
## void clearOnCollisionDetectedCallbacks ( )

Очищает все [added](#addOnCollisionDetectedCallback_CallbackBase_ptr_void) колбэки обнаружения столкновения.
