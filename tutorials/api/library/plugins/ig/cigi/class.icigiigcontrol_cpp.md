# CigiIGControl Class (CPP)

**Header:** #include <plugins/Unigine/CIGIConnector/UnigineCIGIConnector.h>

**Inherits from:** CigiHostPacket


## CigiIGControl Class

### Методы класса

---

## int getDatabase ( ) const

Возвращает значение параметра **Database Number**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Database Number**.
## int getIGMode ( ) const

Возвращает значение параметра **IG Mode**, указанное в пакете.
### Возвращаемое значение

Значение параметра **IG Mode**.
## int getTimeValid ( ) const

Возвращает значение параметра **Timestamp Valid**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Timestamp Valid**.
## int getInterpolation ( ) const

Возвращает значение параметра **Smoothing Enable**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Smoothing Enable**.
## unsigned int getIGFrame ( ) const

Возвращает значение параметра **IG Frame Number**, указанное в пакете.
### Возвращаемое значение

Значение параметра **IG Frame Number**.
## unsigned int getHostFrame ( ) const

Возвращает значение параметра **Host Frame Number**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Host Frame Number**.
## unsigned int getTimeStamp ( ) const

Возвращает значение параметра **Timestamp**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Timestamp**.
