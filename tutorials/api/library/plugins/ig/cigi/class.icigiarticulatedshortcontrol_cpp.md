# CigiArticulatedShortControl Class (CPP)

**Header:** #include <plugins/Unigine/CIGIConnector/UnigineCIGIConnector.h>

**Inherits from:** CigiHostPacket


## CigiArticulatedShortControl Class

### Методы класса

---

## int getEntityID ( ) const

Возвращает ID сущности, указанный в пакете.
### Возвращаемое значение

ID сущности.
## int getPartID1 ( ) const

Возвращает ID сочленённой части 1, указанный в пакете.
### Возвращаемое значение

ID сочленённой части 1.
## int getPartID2 ( ) const

Возвращает ID сочленённой части 2, указанный в пакете.
### Возвращаемое значение

ID сочленённой части 2.
## int getPartEnabled1 ( ) const

Возвращает значение параметра **Articulated Part 1 Enable**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Articulated Part 1 Enable**: 1, если сочленённая часть включена; иначе 0.
## int getPartEnabled2 ( ) const

Возвращает значение параметра **Articulated Part 2 Enable**, указанное в пакете.
### Возвращаемое значение

Значение параметра **Articulated Part 2 Enable**: 1, если сочленённая часть включена; иначе 0.
## int getSelect1 ( ) const

Возвращает значение параметра **DOF Select 1**, указанное в пакете.
### Возвращаемое значение

Значение параметра **DOF Select 1**.
## int getSelect2 ( ) const

Возвращает значение параметра **DOF Select 2**, указанное в пакете.
### Возвращаемое значение

Значение параметра **DOF Select 2**.
## float getDOF1 ( ) const

Возвращает значение параметра **DOF 1**, указанное в пакете.
### Возвращаемое значение

Значение параметра **DOF 1**.
## float getDOF2 ( ) const

Возвращает значение параметра **DOF 2**, указанное в пакете.
### Возвращаемое значение

Значение параметра **DOF 2**.
