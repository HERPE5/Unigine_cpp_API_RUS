# CigiLosExtResponse Class (CPP)

**Header:** #include <plugins/Unigine/CIGIConnector/UnigineCIGIConnector.h>

**Inherits from:** CigiIGPacket


## CigiLosExtResponse Class

### Методы класса

---

## void setLosID ( int id )

Задаёт значение параметра **LOS ID**, указываемое в пакете.
### Аргументы

- *int* **id** - Значение параметра **LOS ID**.

## void setEntityID ( int id )

Задаёт значение параметра **Entity ID**, указываемое в пакете.
### Аргументы

- *int* **id** - Значение параметра **Entity ID**.

## void setEntityValid ( int valid )

Задаёт значение параметра **Entity Valid**, указываемое в пакете.
### Аргументы

- *int* **valid** - Значение параметра **Entity ID Valid**. Поддерживаются следующие значения:

  - 0 - недействительно (если пересечённый полигон не был частью сущности)
  - 1 - действительно (если пересечённый полигон был частью сущности)

## void setResponseValid ( int valid )

Задаёт значение параметра **Response Valid**, указываемое в пакете.
### Аргументы

- *int* **valid** - Значение параметра **Valid**. Поддерживаются следующие значения:

  - 0 - недействительно (пересечение не произошло)
  - 1 - действительно (произошло одно или несколько пересечений)

## void setResponseCount ( int count )

Задаёт значение параметра **Response Count**, указываемое в пакете. Этот параметр указывает общее количество пакетов Line of Sight Response, которые IG вернёт для соответствующего запроса.
### Аргументы

- *int* **count** - Значение параметра **Response Count**.

## void setRangeValid ( int valid )

Задаёт значение параметра **Range Valid**, указываемое в пакете.
### Аргументы

- *int* **valid** - Значение параметра **Range Valid**. Поддерживаются следующие значения: диапазон будет недействительным, если пересечение происходит до минимального диапазона или за пределами максимального диапазона, указанного в запросе LOS vector. Диапазон также будет недействительным, если этот пакет является ответом на запрос LOS segment.

  - 0 - недействительно
  - 1 - действительно

## void setVisible ( int v )

Задаёт значение параметра **Visible**, указываемое в пакете.
### Аргументы

- *int* **v** - Значение параметра **Visible**. Поддерживаются следующие значения:

  - 0 - скрыто (не видно)
  - 1 - видно

## void setRange ( double r )

Задаёт значение параметра **Range**, указываемое в пакете.
### Аргументы

- *double* **r** - Значение параметра **Range**.

## void setPosition ( const Math::dvec3& p )

Задаёт значения параметров, определяющих **Position**, указываемых в пакете.
### Аргументы

- *const  Math::dvec3&* **p** - Координаты положения сущности.

  - Для геодезической системы координат: (Lat, Lon, Alt)
  - Для системы координат сущности: (X-offset, Y-offset, Z-offset)

## void setColor ( const Math::vec4& c )

Задаёт значения параметров, определяющих **Color** поверхности в точке пересечения, указываемых в пакете.
### Аргументы

- *const  Math::vec4&* **c** - Цвет поверхности в точке пересечения: (Red, Green, Blue, Alpha).

## void setAzimuth ( float a )

Задаёт значение параметра **Normal Vector Azimuth**, указываемое в пакете.
### Аргументы

- *float* **a** - Значение параметра **Normal Vector Azimuth**.

## void setElevation ( float e )

Задаёт значение параметра **Normal Vector Elevation**, указываемое в пакете.
### Аргументы

- *float* **e** - Значение параметра **Normal Vector Elevation**.

## void setMaterialCode ( unsigned int code )

Задаёт значение параметра **Material Code**, указываемое в пакете.
### Аргументы

- *unsigned int* **code** - Значение параметра **Material Code**.
