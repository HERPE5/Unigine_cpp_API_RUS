# Unigine::ArrayMap::Iterator Class (CPP)

**Header:** #include <UnigineInterpreter.h>


## ArrayMap::Iterator Class

### Методы класса

---

## Iterator ( )

Конструктор итератора по умолчанию.
## Iterator ( const ArrayMap::Iterator & it )

Конструктор копирования итератора.
### Аргументы

- *const ArrayMap::Iterator &* **it** - Копируемый экземпляр итератора.

## const Variable & get ( )

Возвращает данные итератора.
### Возвращаемое значение

Данные итератора.
## const Variable & key ( )

Возвращает ключ итератора.
### Возвращаемое значение

Ключ итератора.
## int operator!= ( const ArrayMap::Iterator & it )

Сравнение итераторов на неравенство.
### Аргументы

- *const ArrayMap::Iterator &* **it** - Второй итератор для сравнения.

### Возвращаемое значение

Результат операции.
## ArrayMap::Iterator & operator++ ( )

Увеличивает позицию итератора (выполняет операцию предынкремента: *++iterator*).
### Возвращаемое значение

Результат операции.
## ArrayMap::Iterator operator++ ( int )

Увеличивает позицию итератора (выполняет операцию постинкремента: *iterator++*)
### Аргументы

- *int*

### Возвращаемое значение

Результат операции.
## ArrayMap::Iterator operator-- ( int )

Уменьшает позицию итератора (выполняет операцию постдекремента: *iterator--*).
### Аргументы

- *int*

### Возвращаемое значение

Результат операции.
## ArrayMap::Iterator & operator-- ( )

Уменьшает позицию итератора (выполняет операцию предекремента: *--iterator*)
### Возвращаемое значение

Результат операции.
## ArrayMap::Iterator & operator= ( const ArrayMap::Iterator & it )

Оператор присваивания для итератора.
### Аргументы

- *const ArrayMap::Iterator &* **it** - Оператор присваивания для итератора.

### Возвращаемое значение

Присвоенный итератор.
## int operator== ( const ArrayMap::Iterator & it )

Сравнение итераторов на равенство.
### Аргументы

- *const ArrayMap::Iterator &* **it** - Второй итератор.

### Возвращаемое значение

Результат.
