# Unigine.LandscapeMapFileSettings Class (CPP)

**Header:** #include <UnigineObjects.h>


Этот класс используется для загрузки, изменения и применения настроек [landscape map](../../../../api/library/objects/landscape_terrain/class.landscapelayermap_cpp.md), хранящихся в файле `.lmap`.


### См. также


- C++ sample


## LandscapeMapFileSettings Class

### Методы класса

---

## static LandscapeMapFileSettingsPtr create ( )

Конструктор LandscapeMapFileSettings.
## void setHeightBlending ( Landscape::BLENDING_MODE blending )

Задаёт новый режим смешивания, используемый для данных высот карты landscape.
### Аргументы

- *[Landscape::BLENDING_MODE](../../../../api/library/objects/landscape_terrain/class.landscape_cpp.md#BLENDING_MODE)* **blending** - Режим смешивания, используемый для данных высот. Одно из следующих значений:

  - Alpha Blend
  - Additive

## Landscape::BLENDING_MODE getHeightBlending ( ) const

Возвращает текущий режим смешивания, используемый для данных высот карты landscape.
### Возвращаемое значение

Режим смешивания, используемый для данных высот. Одно из следующих значений:
- Alpha Blend
- Additive


## void setAlbedoBlending ( Landscape::BLENDING_MODE blending )

Задаёт новый режим смешивания, используемый для данных альбедо карты landscape.
### Аргументы

- *[Landscape::BLENDING_MODE](../../../../api/library/objects/landscape_terrain/class.landscape_cpp.md#BLENDING_MODE)* **blending** - Режим смешивания, используемый для данных альбедо. Одно из следующих значений:

  - Alpha Blend
  - Additive
  - Overlay
  - Multiplicative

## Landscape::BLENDING_MODE getAlbedoBlending ( ) const

Возвращает текущий режим смешивания, используемый для данных альбедо карты landscape.
### Возвращаемое значение

Режим смешивания, используемый для данных альбедо. Одно из следующих значений:
- Alpha Blend
- Additive
- Overlay
- Multiplicative


## void setMaskBlending ( int mask , Landscape::BLENDING_MODE blend )

Задаёт новый режим смешивания, используемый для данных указанной детализирующей маски.
### Аргументы

- *int* **mask** - Номер детализирующей маски в диапазоне **[0; 19]**.
- *[Landscape::BLENDING_MODE](../../../../api/library/objects/landscape_terrain/class.landscape_cpp.md#BLENDING_MODE)* **blend** - Режим смешивания, используемый для данных указанной детализирующей маски. Одно из следующих значений:

  - Alpha Blend
  - Additive
  - Overlay
  - Multiplicative

## Landscape::BLENDING_MODE getMaskBlending ( int mask ) const

Возвращает текущий режим смешивания, используемый для данных указанной детализирующей маски.
### Аргументы

- *int* **mask** - Номер детализирующей маски в диапазоне **[0; 19]**.

### Возвращаемое значение

Режим смешивания, используемый для данных указанной детализирующей маски. Одно из следующих значений:
- Alpha Blend
- Additive
- Overlay
- Multiplicative


## void setEnabledHeight ( bool height )

Задаёт значение, указывающее, используются ли данные высот карты слоя landscape.
### Аргументы

- *bool* **height** - true, чтобы включить использование данных высот карты слоя landscape, false — чтобы отключить.

## bool isEnabledHeight ( ) const

Возвращает значение, указывающее, используются ли данные высот карты слоя landscape.
### Возвращаемое значение

true, если данные высот карты слоя landscape используются; иначе false.
## void setEnabledAlbedo ( bool albedo )

Задаёт значение, указывающее, используются ли данные альбедо карты слоя landscape.
### Аргументы

- *bool* **albedo** - true, чтобы включить использование данных альбедо карты слоя landscape, false — чтобы отключить.

## bool isEnabledAlbedo ( ) const

Возвращает значение, указывающее, используются ли данные альбедо карты слоя landscape.
### Возвращаемое значение

true, если данные альбедо карты слоя landscape используются; иначе false.
## void setEnabledMask ( int mask , bool enabled )

Задаёт значение, указывающее, используются ли данные детализирующей маски с указанным номером.
### Аргументы

- *int* **mask** - Номер детализирующей маски в диапазоне **[0; 19]**.
- *bool* **enabled** - true, чтобы включить использование данных указанной детализирующей маски, false — чтобы отключить.

## bool isEnabledMask ( int mask ) const

Возвращает значение, указывающее, используются ли данные детализирующей маски с указанным номером.
### Аргументы

- *int* **mask** - Номер детализирующей маски в диапазоне **[0; 19]**.

### Возвращаемое значение

true, если данные детализирующей маски с указанным номером используются; иначе false.
## void setEnabledOpacityHeight ( bool height )

Задаёт значение, указывающее, используется ли информация о непрозрачности данных высот карты слоя landscape. Информация о непрозрачности необходима для смешивания данных нескольких карт слоёв landscape.
### Аргументы

- *bool* **height** - true, чтобы включить использование информации о непрозрачности данных высот карты слоя landscape, false — чтобы отключить.

## bool isEnabledOpacityHeight ( ) const

Возвращает значение, указывающее, используется ли информация о непрозрачности данных высот карты слоя landscape. Информация о непрозрачности необходима для смешивания данных нескольких карт слоёв landscape.
### Возвращаемое значение

true, если информация о непрозрачности данных высот карты слоя landscape используется; иначе false.
## void setEnabledOpacityAlbedo ( bool albedo )

Задаёт значение, указывающее, используется ли информация о непрозрачности данных альбедо карты слоя landscape. Информация о непрозрачности необходима для смешивания данных нескольких карт слоёв landscape.
### Аргументы

- *bool* **albedo** - true, чтобы включить использование информации о непрозрачности данных альбедо карты слоя landscape, false — чтобы отключить.

## bool isEnabledOpacityAlbedo ( ) const

Возвращает значение, указывающее, используется ли информация о непрозрачности данных альбедо карты слоя landscape. Информация о непрозрачности необходима для смешивания данных нескольких карт слоёв landscape.
### Возвращаемое значение

true, если информация о непрозрачности данных альбедо карты слоя landscape используется; иначе false.
## void setEnabledOpacityMask ( int mask , bool enabled )

Задаёт значение, указывающее, используется ли информация о непрозрачности данных высот детализирующей маски с указанным номером. Информация о непрозрачности необходима для смешивания данных нескольких карт слоёв landscape.
### Аргументы

- *int* **mask** - Номер детализирующей маски в диапазоне **[0; 19]**.
- *bool* **enabled** - true, чтобы включить использование информации о непрозрачности данных указанной детализирующей маски, false — чтобы отключить.

## bool isEnabledOpacityMask ( int mask ) const

Возвращает значение, указывающее, используется ли информация о непрозрачности данных детализирующей маски с указанным номером. Информация о непрозрачности необходима для смешивания данных нескольких карт слоёв landscape.
### Аргументы

- *int* **mask** - Номер детализирующей маски в диапазоне **[0; 19]**.

### Возвращаемое значение

true, если информация о непрозрачности данных детализирующей маски с указанным номером используется; иначе false.
## Math:: ivec2 getTilesSize ( ) const

Возвращает размер тайлов карты landscape.
### Возвращаемое значение

Двухкомпонентный вектор (X, Y), представляющий количество тайлов карты слоя landscape по осям X и Y.
## Math:: ivec2 getResolution ( ) const

Возвращает разрешение карты слоя landscape.
### Возвращаемое значение

Двухкомпонентный вектор (X, Y), представляющий разрешение карты слоя landscape по осям X и Y, в пикселях.
## UGUID getGUID ( ) const

Возвращает [GUID](../../../../api/library/filesystem/class.uguid_cpp.md) файла `.lmap`, содержащего данные карты landscape.
### Возвращаемое значение

[GUID](../../../../api/library/filesystem/class.uguid_cpp.md) файла `.lmap`.
## bool isLoaded ( ) const

Возвращает значение, указывающее, загружен ли файл карты landscape (`.lmap`).
### Возвращаемое значение

true, если файл карты landscape (`.lmap`) загружен; иначе false.
## bool load ( const UGUID & guid )

Загружает настройки карты landscape из файла с указанным GUID.
### Аргументы

- *const [UGUID](../../../../api/library/filesystem/class.uguid_cpp.md) &* **guid** - [GUID](../../../../api/library/filesystem/class.uguid_cpp.md) файла `.lmap`, содержащего данные карты landscape.

### Возвращаемое значение

true, если настройки карты landscape успешно загружены из файла с указанным GUID; иначе false.
## bool apply ( )

Применяет все настройки, хранящиеся в файле карты landscape (`.lmap`).
### Возвращаемое значение

true, если настройки файла карты landscape успешно применены; иначе false.
## void setHeightFadeAttenuation ( const Math:: vec2 & attenuation )

Задаёт новое затухание прозрачности, используемое для данных высот карты landscape. Этот параметр определяет расстояние затухания прозрачности, начиная от края карты.
### Аргументы

- *const  Math::[vec2](../../../../api/library/math/class.vec2_cpp.md) &* **attenuation** - Двухкомпонентный вектор **(X, Y)**, определяющий затухание прозрачности данных высот по осям X и Y.

## Math:: vec2 getHeightFadeAttenuation ( ) const

Возвращает текущее затухание прозрачности для данных высот карты landscape. Этот параметр определяет расстояние затухания прозрачности, начиная от края карты.
### Возвращаемое значение

Двухкомпонентный вектор **(X, Y)**, определяющий затухание прозрачности данных высот по осям X и Y.
## void setAlbedoFadeAttenuation ( const Math:: vec2 & attenuation )

Задаёт новое затухание прозрачности, используемое для данных альбедо карты landscape. Этот параметр определяет расстояние затухания прозрачности, начиная от края карты.
### Аргументы

- *const  Math::[vec2](../../../../api/library/math/class.vec2_cpp.md) &* **attenuation** - Двухкомпонентный вектор **(X, Y)**, определяющий затухание прозрачности данных альбедо по осям X и Y.

## Math:: vec2 getAlbedoFadeAttenuation ( ) const

Возвращает текущее затухание прозрачности для данных альбедо карты landscape. Этот параметр определяет расстояние затухания прозрачности, начиная от края карты.
### Возвращаемое значение

Двухкомпонентный вектор **(X, Y)**, определяющий затухание прозрачности данных альбедо по осям X и Y.
## void setMaskFadeAttenuation ( int mask , const Math:: vec2 & fade_attenuation )

Задаёт новое затухание прозрачности, используемое для данных указанной детализирующей маски. Этот параметр определяет расстояние затухания прозрачности, начиная от края карты.
### Аргументы

- *int* **mask** - Номер маски.
- *const  Math::[vec2](../../../../api/library/math/class.vec2_cpp.md) &* **fade_attenuation** - Двухкомпонентный вектор **(X, Y)**, определяющий затухание прозрачности данных детализирующей маски по осям X и Y.

## Math:: vec2 getMaskFadeAttenuation ( int mask ) const

Возвращает текущее затухание прозрачности для данных указанной детализирующей маски. Этот параметр определяет расстояние затухания прозрачности, начиная от края карты.
### Аргументы

- *int* **mask** - Номер маски.

### Возвращаемое значение

Двухкомпонентный вектор **(X, Y)**, определяющий затухание прозрачности данных детализирующей маски по осям X и Y.
## Landscape::COMPRESSOR_TYPE getHeightCompressor ( ) const

Возвращает тип сжатия, используемый в настоящее время для данных высот.
### Возвращаемое значение

Тип сжатия, используемый в настоящее время для данных высот.
## Landscape::COMPRESSOR_TYPE getAlbedoCompressor ( ) const

Возвращает тип сжатия, используемый в настоящее время для данных альбедо.
### Возвращаемое значение

Тип сжатия, используемый в настоящее время для данных альбедо.
## Landscape::COMPRESSOR_TYPE getOpacityHeightCompressor ( ) const

Возвращает тип сжатия, используемый в настоящее время для данных непрозрачности высот.
### Возвращаемое значение

Тип сжатия, используемый в настоящее время для данных непрозрачности высот.
## Landscape::COMPRESSOR_TYPE getMaskCompressor ( int mask )

Возвращает тип сжатия, используемый в настоящее время для данных маски с указанным номером.
### Аргументы

- *int* **mask** - Номер маски.

### Возвращаемое значение

Тип сжатия, используемый в настоящее время для данных маски с указанным номером.
## Landscape::COMPRESSOR_TYPE getMaskOpacityCompressor ( int mask )

Возвращает тип сжатия, используемый в настоящее время для данных непрозрачности маски с указанным номером.
### Аргументы

- *int* **mask** - Номер маски.

### Возвращаемое значение

Тип сжатия, используемый в настоящее время для данных непрозрачности маски с указанным номером.
## bool isCompressed ( ) const

Возвращает значение, указывающее, сжат ли файл `.lmap`.
### Возвращаемое значение

true, если файл сжат; иначе false.
## bool isEnabledAlbedoTextureCompression ( ) const

Возвращает значение, указывающее, включено ли сжатие текстуры альбедо.
### Возвращаемое значение

true, если сжатие текстуры альбедо включено; иначе false.
## bool isEnabledHeightTextureCompression ( ) const

Возвращает значение, указывающее, включено ли сжатие текстуры высот.
### Возвращаемое значение

true, если сжатие текстуры высот включено; иначе false.
## bool isEnabledOpacityHeightTextureCompression ( ) const

Возвращает значение, указывающее, включено ли сжатие текстуры непрозрачности высот.
### Возвращаемое значение

true, если сжатие текстуры непрозрачности высот включено; иначе false.
## bool isEnabledMaskTextureCompression ( int mask )

Возвращает значение, указывающее, включено ли сжатие текстуры маски с указанным номером.
### Аргументы

- *int* **mask** - Номер маски.

### Возвращаемое значение

true, если сжатие текстуры маски с указанным номером включено; иначе false.
## bool isEnabledMaskOpacityTextureCompression ( int mask )

Возвращает значение, указывающее, включено ли сжатие текстуры непрозрачности маски с указанным номером.
### Аргументы

- *int* **mask** - Номер маски.

### Возвращаемое значение

true, если сжатие текстуры непрозрачности маски с указанным номером включено; иначе false.
