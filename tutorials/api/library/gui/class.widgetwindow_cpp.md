# Unigine::WidgetWindow Class (CPP)

**Header:** #include <UnigineWidgets.h>

**Inherits from:** Widget


Этот класс создаёт [titled window](../../../code/gui/ui/ui_containers.md#window).


Объект этого класса выглядит следующим образом:


![](../../../code/gui/ui/widgets/window.png)


По умолчанию окно, созданное с помощью этого класса, состоит из [single vertical column container](../../../api/library/gui/class.widgetvbox_cpp.md) и области перетаскивания сверху.


Класс WidgetWindow предоставляет методы, позволяющие [editing the drag area](#setDragAreaEnabled_int_void). Например:


<details>
<summary>AppSystemLogic.cpp | Закрыть</summary>

`AppSystemLogic.cpp`


```cpp
#include "AppSystemLogic.h"
#include <UnigineWindowManager.h>

using namespace Unigine;

// экземпляр класса EventConnections для управления подписками на события
EventConnections econnections;

EngineWindowViewportPtr window;

void on_close()
{
	window.deleteLater();
}

int AppSystemLogic::init()
{

		// дополнительное окно без границ
		window = EngineWindowViewport::create(Math::ivec2(512, 256));
		window->setBordersEnabled(false);

		GuiPtr gui = window->getSelfGui();

		// создание виджета окна
		WidgetWindowPtr widget_window = WidgetWindow::create(gui, "Borderless Window");
		widget_window->setSizeable(false);
		widget_window->setMoveable(false);
		window->addChild(widget_window, Gui::ALIGN_EXPAND);

		// добавление иконки закрытия в окно
		WidgetIconPtr close_icon = WidgetIcon::create(gui, "window_close.png");
		close_icon->setPosition(10, -24);
		close_icon->getEventClicked().connect(econnections, on_close);
		widget_window->addChild(close_icon, Gui::ALIGN_OVERLAP | Gui::ALIGN_RIGHT | Gui::ALIGN_TOP);

		// настройка области перетаскивания окна так, чтобы она не пересекалась с иконкой закрытия
		widget_window->setDragAreaPadding(3, 30, 3, 0);
		widget_window->setDragAreaBackground(1);
		widget_window->setDragAreaBackgroundColor(Math::vec4(1.0f, 0.0f, 0.0f, 0.3f));

		window->setPosition(Math::ivec2(100, 100));
		window->show();

	return 1;
}

int AppSystemLogic::shutdown()
{
	// удаление всех подписок на события
	econnections.disconnectAll();

	return 1;
}


```

</details>


Окно будет отображаться следующим образом:


![](drag_area.png)


### См. также


- C++ sample


## WidgetWindow Class

### Методы класса

## void setText ( const char * text )

Задаёт новый заголовок окна.
### Аргументы

- *const char ** **text** - Заголовок окна.

## const char * getText () const

Возвращает текущий заголовок окна.
### Возвращаемое значение

Текущий заголовок окна.
## void setTextAlign ( )

Задаёт новое выравнивание заголовка окна.
### Аргументы

- **align** - Одна из предопределённых переменных **[ALIGN_*](../../../api/library/gui/class.gui_cpp.md#ALIGN_BACKGROUND)**.

## getTextAlign () const

Возвращает текущее выравнивание заголовка окна.
### Возвращаемое значение

Текущая одна из предопределённых переменных **[ALIGN_*](../../../api/library/gui/class.gui_cpp.md#ALIGN_BACKGROUND)**.
## int getPaddingBottom () const

Возвращает текущий отступ снизу для содержимого виджета.
### Возвращаемое значение

Текущий отступ снизу в пикселях.
## int getPaddingTop () const

Возвращает текущий отступ сверху для содержимого виджета.
### Возвращаемое значение

Текущий отступ сверху в пикселях.
## int getPaddingRight () const

Возвращает текущий отступ справа для содержимого виджета.
### Возвращаемое значение

Текущий отступ справа в пикселях.
## int getPaddingLeft () const

Возвращает текущий отступ слева для содержимого виджета.
### Возвращаемое значение

Текущий отступ слева в пикселях.
## int getSpaceY () const

Возвращает текущее вертикальное расстояние между виджетами в окне, а также между ними и границей окна.
### Возвращаемое значение

Текущее вертикальное расстояние.
## int getSpaceX () const

Возвращает текущее горизонтальное расстояние между виджетами в окне, а также между ними и границей окна.
### Возвращаемое значение

Текущее горизонтальное расстояние.
## void setMaxHeight ( int height )

Задаёт новое максимальное значение высоты окна.
### Аргументы

- *int* **height** - Максимальное значение высоты.

## int getMaxHeight () const

Возвращает текущее максимальное значение высоты окна.
### Возвращаемое значение

Текущее максимальное значение высоты.
## void setMaxWidth ( int width )

Задаёт новое максимальное значение ширины окна.
### Аргументы

- *int* **width** - Максимальное значение ширины.

## int getMaxWidth () const

Возвращает текущее максимальное значение ширины окна.
### Возвращаемое значение

Текущее максимальное значение ширины.
## void setTransform ( const const Math:: mat4 && transform )

Задаёт новую глобальную матрицу трансформации виджета.
### Аргументы

- *const const  Math::[mat4](../../../api/library/math/class.mat4_cpp.md) &&* **transform** - Матрица трансформации.

## const Math:: mat4 & getTransform () const

Возвращает текущую глобальную матрицу трансформации виджета.
### Возвращаемое значение

Текущая матрица трансформации.
## void setColor ( )

Задаёт новый цвет для глобального множителя цвета. По умолчанию соответствует vec4(1,1,1,1) (белый).
### Аргументы

- **color** - Цвет множителя.

## getColor () const

Возвращает текущий цвет для глобального множителя цвета. По умолчанию соответствует vec4(1,1,1,1) (белый).
### Возвращаемое значение

Текущий цвет множителя.
## void setSnapDistance ( int distance )

Задаёт новое расстояние, на котором окно прилипает к другому окну или краю экрана. По умолчанию 0 (не прилипать).
### Аргументы

- *int* **distance** - Расстояние в пикселях.

## int getSnapDistance () const

Возвращает текущее расстояние, на котором окно прилипает к другому окну или краю экрана. По умолчанию 0 (не прилипать).
### Возвращаемое значение

Текущее расстояние в пикселях.
## void setFloatable ( bool floatable )

Задаёт новое значение, указывающее, анимируется ли окно при переходе в свёрнутое состояние и обратно. По умолчанию эта опция отключена.
### Аргументы

- *bool* **floatable** - Значение **true** включает анимацию окна; **false** - отключает её.

## bool isFloatable () const

Возвращает текущее значение, указывающее, анимируется ли окно при переходе в свёрнутое состояние и обратно. По умолчанию эта опция отключена.
### Возвращаемое значение

**true**, если анимация окна включена; иначе **false**.
## void setBlendable ( bool blendable )

Задаёт новое значение, указывающее, может ли окно плавно появляться и исчезать при переходе в свёрнутое состояние и обратно. По умолчанию эта опция отключена.
### Аргументы

- *bool* **blendable** - Значение **true** включает возможность плавного появления и исчезновения окна; **false** - отключает её.

## bool isBlendable () const

Возвращает текущее значение, указывающее, может ли окно плавно появляться и исчезать при переходе в свёрнутое состояние и обратно. По умолчанию эта опция отключена.
### Возвращаемое значение

**true**, если возможность плавного появления и исчезновения окна включена; иначе **false**.
## void setTitleable ( bool titleable )

Задаёт новое значение, указывающее, сворачивается ли окно при двойном щелчке по нему. По умолчанию эта опция отключена.
### Аргументы

- *bool* **titleable** - Значение **true** включает сворачивание окна по двойному щелчку; **false** - отключает его.

## bool isTitleable () const

Возвращает текущее значение, указывающее, сворачивается ли окно при двойном щелчке по нему. По умолчанию эта опция отключена.
### Возвращаемое значение

**true**, если сворачивание окна по двойному щелчку включено; иначе **false**.
## void setSizeable ( bool sizeable )

Задаёт новое значение, указывающее, можно ли изменять размер окна. По умолчанию эта опция отключена.
### Аргументы

- *bool* **sizeable** - Значение **true** включает возможность изменения размера окна; **false** - отключает её.

## bool isSizeable () const

Возвращает текущее значение, указывающее, можно ли изменять размер окна. По умолчанию эта опция отключена.
### Возвращаемое значение

**true**, если возможность изменения размера окна включена; иначе **false**.
## void setMoveable ( bool moveable )

Задаёт новое значение, указывающее, можно ли перемещать окно. По умолчанию эта опция отключена.
### Аргументы

- *bool* **moveable** - Значение **true** включает возможность перемещения окна; **false** - отключает её.

## bool isMoveable () const

Возвращает текущее значение, указывающее, можно ли перемещать окно. По умолчанию эта опция отключена.
### Возвращаемое значение

**true**, если возможность перемещения окна включена; иначе **false**.
## void setBorderColor ( )

Задаёт новый цвет границы виджета.
### Аргументы

- **color** - Четырёхкомпонентный вектор, задающий цвет в формате RGBA.

## getBorderColor () const

Возвращает текущий цвет границы виджета.
### Возвращаемое значение

Текущий четырёхкомпонентный вектор, задающий цвет в формате RGBA.
## void setBackgroundColor ( )

Задаёт новый используемый цвет фона виджета.
### Аргументы

- **color** - Четырёхкомпонентный вектор, задающий цвет в формате RGBA.

## getBackgroundColor () const

Возвращает текущий используемый цвет фона виджета.
### Возвращаемое значение

Текущий четырёхкомпонентный вектор, задающий цвет в формате RGBA.
## void setDragAreaEnabled ( bool enabled )

Задаёт новое значение, указывающее, включена ли область перетаскивания окна.
### Аргументы

- *bool* **enabled** - Значение **true** включает область перетаскивания; **false** - отключает её.

## bool isDragAreaEnabled () const

Возвращает текущее значение, указывающее, включена ли область перетаскивания окна.
### Возвращаемое значение

**true**, если область перетаскивания включена; иначе **false**.
## int getDragAreaPaddingTop () const

Возвращает текущий отступ сверху для области перетаскивания.
### Возвращаемое значение

Текущий отступ сверху в пикселях.
## int getDragAreaPaddingRight () const

Возвращает текущий отступ справа для области перетаскивания.
### Возвращаемое значение

Текущий отступ справа в пикселях.
## void setDragAreaBackgroundColor ( const Math:: vec4 & color )

Задаёт новый цвет фона области перетаскивания.
### Аргументы

- *const  Math::[vec4](../../../api/library/math/class.vec4_cpp.md)&* **color** - Цвет фона.

## Math:: vec4 getDragAreaBackgroundColor () const

Возвращает текущий цвет фона области перетаскивания.
### Возвращаемое значение

Текущий цвет фона.
## void setDragAreaBackground ( int background )

Задаёт новое значение, указывающее, включён ли рендеринг фона для области перетаскивания.
### Аргументы

- *int* **background** - Значение, указывающее, включён ли рендеринг фона (**1**, если рендеринг включён; иначе **0**).

## int getDragAreaBackground () const

Возвращает текущее значение, указывающее, включён ли рендеринг фона для области перетаскивания.
### Возвращаемое значение

Текущее значение, указывающее, включён ли рендеринг фона (**1**, если рендеринг включён; иначе **0**).
## int getDragAreaPaddingLeft () const

Возвращает текущий отступ слева для области перетаскивания.
### Возвращаемое значение

Текущий отступ слева в пикселях.
## int getDragAreaPaddingBottom () const

Возвращает текущий отступ снизу для области перетаскивания.
### Возвращаемое значение

Текущий отступ снизу в пикселях.
## void setGlobalMovement ( bool movement )

Задаёт новое значение, определяющее, можно ли перемещать окно по всему GUI или только в пределах родительского виджета. По умолчанию эта опция отключена, и окно можно перемещать только относительно его родителя.
### Аргументы

- *bool* **movement** - Значение **true** включает перемещение окна по всему GUI; **false** - отключает его.

## bool isGlobalMovement () const

Возвращает текущее значение, определяющее, можно ли перемещать окно по всему GUI или только в пределах родительского виджета. По умолчанию эта опция отключена, и окно можно перемещать только относительно его родителя.
### Возвращаемое значение

**true**, если перемещение окна по всему GUI включено; иначе **false**.
## int getMinTextureWidth () const

Возвращает текущую минимальную ширину рендера окна - ширину текстуры окна из скина GUI.
### Возвращаемое значение

Текущая минимальная ширина рендера окна, в пикселях.
## int getTextWidth () const

Возвращает текущую ширину заголовка окна.
### Возвращаемое значение

Текущая ширина заголовка окна, в пикселях.
## int getTextHeight () const

Возвращает текущую высоту заголовка окна.
### Возвращаемое значение

Текущая высота заголовка окна, в пикселях.
## void setOutBoundsCallbacksEnabled ( bool enabled )

Задаёт новое значение, определяющее, включена ли обработка колбэков для дочерних виджетов окна, когда курсор находится за пределами окна.
### Аргументы

- *bool* **enabled** - Значение **true** включает обработку колбэков для дочерних виджетов, если курсор находится за пределами окна; **false** - отключает её.

## bool isOutBoundsCallbacksEnabled () const

Возвращает текущее значение, определяющее, включена ли обработка колбэков для дочерних виджетов окна, когда курсор находится за пределами окна.
### Возвращаемое значение

**true**, если обработка колбэков для дочерних виджетов, когда курсор находится за пределами окна, включена; иначе **false**.
## int getMinTextureHeight () const

Возвращает текущую минимальную высоту рендера окна - высоту текстуры окна из скина GUI.
### Возвращаемое значение

Текущая минимальная высота рендера окна, в пикселях.
## void setStencil ( int stencil )

Задаёт новое значение, указывающее, обрезает ли виджет свои дочерние элементы по установленным [bounds](../../../api/library/gui/class.widget_cpp.md#setWidth_int_void). Всё, что выходит за их пределы, не отображается. Эта опция работает только если у дочерних элементов установлен флаг [ALIGN_OVERLAP](../../../api/library/gui/class.gui_cpp.md#ALIGN_OVERLAP) (иначе они расширят границы контейнера, и обрезки не будет).
### Аргументы

- *int* **stencil** - **1**, если обрезка включена; иначе **0**.

## int getStencil () const

Возвращает текущее значение, указывающее, обрезает ли виджет свои дочерние элементы по установленным [bounds](../../../api/library/gui/class.widget_cpp.md#setWidth_int_void). Всё, что выходит за их пределы, не отображается. Эта опция работает только если у дочерних элементов установлен флаг [ALIGN_OVERLAP](../../../api/library/gui/class.gui_cpp.md#ALIGN_OVERLAP) (иначе они расширят границы контейнера, и обрезки не будет).
### Возвращаемое значение

Текущее значение: **1**, если обрезка включена; иначе **0**.
---

## static WidgetWindowPtr create ( const Ptr < Gui > & gui , const char * str = 0 , int x = 0 , int y = 0 )

Конструктор. Создаёт окно с заданными параметрами и добавляет его в указанный GUI.
### Аргументы

- *const [Ptr](../../../api/library/common/class.ptr_cpp.md)<[Gui](../../../api/library/gui/class.gui_cpp.md)> &* **gui** - [GUI](../../../api/library/gui/class.gui_cpp.md), которому будет принадлежать новое окно.
- *const char ** **str** - Заголовок окна. Это необязательный параметр.
- *int* **x** - Горизонтальное расстояние между виджетами в окне, а также между ними и границей окна. Это необязательный параметр.
- *int* **y** - Вертикальное расстояние между виджетами в окне, а также между ними и границей окна. Это необязательный параметр.

## static WidgetWindowPtr create ( const char * str = 0 , int x = 0 , int y = 0 )

Конструктор. Создаёт окно с заданными параметрами и добавляет его в GUI движка.
### Аргументы

- *const char ** **str** - Заголовок окна. Это необязательный параметр.
- *int* **x** - Горизонтальное расстояние между виджетами в окне, а также между ними и границей окна. Это необязательный параметр.
- *int* **y** - Вертикальное расстояние между виджетами в окне, а также между ними и границей окна. Это необязательный параметр.

## void setPadding ( int l , int r , int t , int b )

Задаёт отступы виджета со всех сторон. Отступ освобождает область вокруг содержимого виджета (внутри него).
### Аргументы

- *int* **l** - Отступ слева в пикселях.
- *int* **r** - Отступ справа в пикселях.
- *int* **t** - Отступ сверху в пикселях.
- *int* **b** - Отступ снизу в пикселях.

## void setSpace ( int x , int y )

Задаёт расстояние между виджетами в окне, а также между ними и границей окна.
### Аргументы

- *int* **x** - Горизонтальное расстояние. Если задано отрицательное значение, вместо него будет использовано 0.
- *int* **y** - Вертикальное расстояние. Если задано отрицательное значение, вместо него будет использовано 0.

## void setDragAreaPadding ( int l , int r , int t , int b )

Задаёт отступы для всех сторон области перетаскивания.
### Аргументы

- *int* **l** - Отступ слева в пикселях.
- *int* **r** - Отступ справа в пикселях.
- *int* **t** - Отступ сверху в пикселях.
- *int* **b** - Отступ снизу в пикселях.
